#include <stdio.h>
#include <string.h>
#include <stdlib.h>

void main() {

    int nTestes;
    int i;
    scanf("%d", &nTestes);

    for (i = 0; i < nTestes; i++) {
        int n;
        int j;
        scanf("%d", &n);
        for (j = 0; j < n; j++) {
            char opt;
            int e;

            scanf("%c", &opt);
            if (strcmp())
        }

    }



}